name: "Contracts"

on:
  push:

jobs:
  contracts-splits:
    name: Revolution Splits
    uses: ./.github/workflows/splits_contracts.yml
    secrets: inherit

  contracts-utils:
    name: Utility Contracts
    uses: ./.github/workflows/utils_contracts.yml
    secrets: inherit
